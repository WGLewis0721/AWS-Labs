# Model 2+3 Terraform Apply - 2026-04-07

## Summary

Applied the saved Terraform plan for the Model 2+3 codification from `terraform-aws/environments/dev`.

Final result:

- `terraform apply tfplan` succeeded.
- Apply summary: `25 imported, 0 added, 1 changed, 0 destroyed`.
- Post-apply verification plan returned: `No changes. Your infrastructure matches the configuration.`

## Steps Taken

1. Read required workspace instructions:
   - `artifacts/prompts/copilot-instructions-v1.md`
   - `artifacts/prompts/Seasoning.md`
2. Reviewed local skills under `artifacts/skills`.
3. Used Terraform-specific skills:
   - local `artifacts/skills/terraform-skill.md`
   - local `artifacts/skills/aws-cli-skill.md`
   - local `artifacts/skills/network-troubleshooting/SKILL.md`
   - external Terraform best-practices and style-guide skills
4. Confirmed AWS caller identity:
   - account `394281571385`
   - principal `arn:aws:iam::394281571385:user/Deployer`
   - region `us-east-1`
5. Ran `terraform fmt -check -recursive` from `terraform-aws`.
6. Ran `terraform validate` from `terraform-aws/environments/dev`.
7. Ran `terraform plan -out=tfplan -no-color`.
8. Parsed the saved plan JSON before apply:
   - hard-stop destroy/replacement count: `0`
   - replacement count: `0`
   - destroy count from plan summary: `0`
9. Ran `terraform apply tfplan`.
10. Ran a post-apply `terraform plan -no-color`.

## Fixes Applied

- Imported 25 live Model 2+3 resources into Terraform state:
  - four TGW route tables
  - nine TGW routes
  - six TGW route table associations
  - six NACL rules
- Removed old generic TGW route table association state tracking through the configured `removed` block with `destroy = false`.
- Updated `module.compute.aws_instance.this["c1_portal"]` in place:
  - `user_data` hash updated
  - `user_data_replace_on_change` changed from `true` to `false`
  - instance was not replaced

## Problems Encountered

- The first PowerShell JSON inspection used `ConvertFrom-Json -Depth`, which is unsupported in this host's PowerShell version. It was rerun without `-Depth` before apply.
- Terraform emitted a backend warning:
  - `The parameter "dynamodb_table" is deprecated. Use parameter "use_lockfile" instead.`
  - This did not block the plan or apply.

## Evidence

- Plan log: `artifacts/results/model23-apply-plan-20260407.txt`
- Apply log: `artifacts/results/model23-apply-20260407.txt`
- Post-apply plan log: `artifacts/results/model23-postapply-plan-20260407.txt`

Key Terraform outputs after apply:

- `a2_linux_public_ip`: `44.204.129.98`
- `c1_portal` instance ID: `i-02d981dda3237738c`
- `c1_portal` private IP: `10.2.2.10`
- `b1` private IP: `10.1.3.10`

## Recommended Steps

- Keep `terraform-aws/modules/compute/main.tf`, `terraform-aws/modules/network/main.tf`, `terraform-aws/modules/security/main.tf`, and `terraform-aws/environments/dev/imports-model23.tf` together when committing; they are part of the same state convergence.
- Consider updating the Terraform backend configuration away from deprecated `dynamodb_table` to the current lockfile setting.
- Run the canonical SSM netcheck documents if operator-path validation is needed after this state convergence:
  - `lab-netcheck-a1`
  - `lab-netcheck-a2`
