# dev environment

Root Terraform configuration for development.

## Run

1. Copy `terraform.tfvars.example` to `terraform.tfvars`.
2. Update `backend.tf` values.
3. Run `terraform init`, `terraform plan`, `terraform apply` in this folder.
